# 腾讯云 SCF 部署说明（傻瓜版）

> 目标：把黄金监测部署到腾讯云函数（SCF），实现**关掉自己电脑也照常每 10 分钟监测、到价弹微信**。
> 已备好：`gold_monitor_scf.zip`（含 `gold_monitor.py` / `index.py` / `config.json` / `requirements.txt`）
> 你的 WxPusher 凭证已写进 `config.json`，目标金价已设为 `[880, 900]`。

## 前置条件
- 一个腾讯云账号，开通「云函数 SCF」（按量计费，个人监测基本在免费额度内）。
- 本机解压能力（或直接网页上传 zip）。

## 步骤

### 1. 新建函数
1. 登录 https://console.cloud.tencent.com/scf → 「函数服务」→「新建」。
2. 创建方式：**自定义创建**。
3. 填写：
   - 函数类型：**事件函数**
   - 函数名称：`gold-monitor`
   - 地域：就近（如广州）
   - 运行环境：**Python 3.12**
   - 函数代码：**本地上传 zip** → 选择 `gold_monitor_scf.zip`
   - 执行方法：`index.main_handler`
4. 点「完成」，等待上传。

### 2. 处理依赖（关键）
代码用到了 `akshare` 和 `requests`，需要让函数能 import。

- **推荐（最简单）**：函数创建后进入「函数管理 → 函数配置」，打开 **「自动安装依赖」** 开关（SCF 会按 `requirements.txt` 自动 `pip install`）。`akshare` 体积较大，安装可能要 1–2 分钟，属正常。
- **若自动安装超时/失败**：改用「层（Layer）」——本地把 `akshare`+`requests` 的 `site-packages` 打成 zip 上传为 Python 层，并在函数里绑定该层。

### 3. 配置定时触发器（让它每 10 分钟跑一次）
1. 「触发管理」→「创建触发器」。
2. 触发方式：**定时触发**。
3. 触发周期：选「自定义」填 Cron 表达式：
   ```
   */10 * * * *
   ```
   （每 10 分钟触发一次 `main_handler`，即代码里的 `--once` 单次取数+推送）
4. 备注随意，保存。

### 4. 测试
「函数代码」页 → 点「测试」→ 运行。查看日志应出现：
- 取数成功（含 SGE Au99.99 实时价）
- `[push] WxPusher -> 200`（或 `code:1000`）

微信随后应收到一条金价播报，说明链路通了。

### 5. 运行效果
- 平时：每 10 分钟一次**播报**（平时层，间隔 7200s 仅作采样提示）。
- 金价进入 880 / 900 的 **±1% 区间**：自动切**高频层**采样。
- 价格**穿越** 880 或 900：推送 `⚠️ 金价到达目标位` 到价提醒（每次穿越只提醒一次，防刷屏）。

## 状态持久化说明
- 默认状态文件存 `/tmp`（SCF 复用 warm 实例期间有效，个人监测足够）。
- 要 100% 跨实例可靠（实例回收也不丢穿越状态）：启用 `index.py` 末尾的 **COS 持久化**代码，需自建一个 COS 桶 + `cos-python-sdk-v5` 层 + 环境变量 `COS_BUCKET` / `COS_REGION`（取消注释即生效）。

## 注意事项
- 数据源为 `akshare` 免费实时行情，**分钟级延迟**，重大实盘操作请以券商/银行终端为准。
- 实时价仅在**交易时段**更新（日盘 9:00–15:30，夜盘 21:00–02:30），休市时取最后值。
- 若想改目标金价/推送人：改 `config.json` 后**重新打包上传**（或直接在函数代码编辑器里改后部署）。
